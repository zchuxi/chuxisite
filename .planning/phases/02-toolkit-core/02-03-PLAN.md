---
phase: 02
slug: toolkit-core
plan: 03
type: execute
wave: 2
depends_on: ["02-02"]
files_modified:
  - src/pages/tools/index.astro
  - src/pages/tools/[slug].astro
  - src/pages/tools/category/[category].astro
  - src/pages/tools/tag/[tag].astro
requirements: [TOOL-01, TOOL-02, TOOL-03, TOOL-04, TOOL-05]
autonomous: true
created: 2026-07-15
---

# 02-03 · 四张工具库页面（主页 + 详情 + 分类档案 + 标签档案）+ build 验证

## 目标

在 02-01 数据层 + 02-02 组件库就绪后，落位 Phase 2 全部页面：把 `src/pages/tools/index.astro` 从 Phase 1 占位空状态迁为完整工具库主页（summary + 分类 pill 行 + 标签云 + 全部网格）；新建 `/tools/[slug]`（图文详情 + MDX `<Content />` + 底部 nav）、`/tools/category/[category]`（分类档案）、`/tools/tag/[tag]`（标签档案）。全部经 lib 读取 + `<article>` 双层 EntryCard；分类 / 标签走 `getStaticPaths` 静态预生成。

## 需求牵引

| Requirement | 本 plan 落地页 |
|---|---|
| TOOL-01 | 主页卡片网格（全部工具、名/summary/外链按钮/tags）+ D-02 固定排序 |
| TOOL-02 | `/tools/[slug]` 独立详情 + MDX `<Content />` + 面包屑 + 底部 nav |
| TOOL-03 | `/tools/category/[category]` 分类静态档案 |
| TOOL-04 | `/tools/tag/[tag]` 标签静态档案 + tags transform 归一化后聚合 |
| TOOL-05 | 所有外链按钮 target=_blank rel=noopener；hero 外链按钮同 EntryCard 视觉 |

## 任务

<task id="02-03-T1">
<name>主页 index.astro 迁出占位 → 完整工具库主页</name>
<files>src/pages/tools/index.astro</files>
<read_first>
- `.planning/phases/02-toolkit-core/02-CONTEXT.md` §decisions D-02/D-03/D-13/D-14（固定排序、1/2/3 列、主页骨架、不分页）
- `.planning/phases/02-toolkit-core/02-RESEARCH.md` §TOOL-01（主页骨架完整契约）+ §1 getStaticPaths 形式
- `.planning/phases/02-toolkit-core/02-PATTERNS.md` M-4（完整主页 skeleton + import 清单 + tags 聚合 + slugOf 强制导入）
- `.planning/phases/02-toolkit-core/02-UI-SPEC.md` § Layout & Grid（主页垂直骨架 D-13）+ § Typography + § Copywriting
- `.planning/phases/02-toolkit-core/02-UI-SPEC.md` § Copywriting Contract（summary line 文案：「共 {N} 件工具 · 慢慢整理中～」；主页空状态文案）
- `src/pages/index.astro`（closest analog：lib 读取 + BaseLayout + Header + Footer + EntryCard + 计数 badge）
- `src/lib/tools.ts`（T2 getAllTools/slugOf）
</read_first>
<action>
把 `src/pages/tools/index.astro` 从 Phase 1 占位空状态（Divider + 「空空如也」+ 回到首页）迁为完整工具库主页。import BaseLayout + Header + Footer + EntryCard + PillRow + TagCloud + EmptyState + `{ getAllTools, slugOf }`。

frontmatter：
```
const tools = await getAllTools();
const categories = [...new Set(tools.map((t) => t.data.category))];
const tagCounts = new Map<string, number>();
for (const t of tools) for (const tag of t.data.tags) tagCounts.set(tag, (tagCounts.get(tag) ?? 0) + 1);
const tags = [...tagCounts.entries()].sort((a, b) => b[1] - a[1]).map(([tag]) => tag);
```

骨架自上而下（参照 UI-SPEC § Layout & Grid）：

1. `<Header />`
2. `<main class="mx-auto max-w-[72rem] px-4 py-12 md:px-6">`
3. summary line `<p class="text-sm text-[--color-text-muted]">共 {tools.length} 件工具 · 慢慢整理中～</p>`
4. PillRow pills=[首项 fixed `{ href: '/tools', label: '全部工具', active: true }`] + categories.map 生成 `{ href: '/tools/category/${c}', label: c, active: false }`。默认 active 首项；由 page 当前 url === '/tools' 决定（首页时全部 tools；无工具时不显示 pill）
5. TagCloud class="mt-12" tags = tags.map(ta => ({ href: `/tools/tag/${ta}`, label: ta, count: tagCounts.get(ta), active: false }))
6. 空状态分支 `tools.length === 0 ? <EmptyState />` 否则 `<section class="mt-12 grid grid-cols-1 gap-6 md:grid-cols-2 md:gap-6 lg:grid-cols-3 lg:gap-8">` + tools.map 渲染 `<EntryCard href={'/tools/${slugOf(t.data.title)}'} externalUrl={t.data.url} icon="ph:wrench" title={t.data.title} description={t.data.summary} />`
7. 页面底部 Footer 前 `mt-24`（沿用 Phase 1）
8. `<Footer />`
9. BaseLayout `title="工具库"` `description="我收藏与折腾过的好用软件 / 网站，分类整理、图文记录"`

**Ponytail 强制**：map 内 `slugOf(t.data.title)` 必须导入 02-01 T2 导出的 `slugOf`，不内联 `encodeURIComponent`。
</action>
<verify>
- `grep -c "slugOf" src/pages/tools/index.astro` ≥ 1
- `grep -c "PillRow\|TagCloud\|EntryCard" src/pages/tools/index.astro` ≥ 3
- `grep -c "共.*件工具.*慢慢整理中" src/pages/tools/index.astro` ≥ 1
- `grep -c 'grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 lg:gap-8' src/pages/tools/index.astro` ≥ 1
- `npm run build` 通过
</verify>
<acceptance_criteria
`src/pages/tools/index.astro` 自 Phase 1 占位迁出：summary line + 分类 pill 行（首项「全部工具」active）+ 标签云（按 count 降序）+ 全部工具网格（1→2→3 列）；EntryCard 整卡 href 指向 `/tools/${slugOf(title)}`、externalUrl 指向 `url`；空状态复用 EmptyState；build 通过；`astro check` 通过。
</acceptance_criteria>
</task>

<task id="02-03-T2">
<name>详情页 [slug].astro（MDX + breadcrumb + hero 外链按钮 + 底部 nav）</name>
<files>src/pages/tools/[slug].astro</files>
<read_first>
- `.planning/phases/02-toolkit-core/02-CONTEXT.md` §decisions D-09/D-11/D-12（详情路由 slug 派生；底部 nav；MDX <Content /> 渲染心得/优缺点）
- `.planning/phases/02-toolkit-core/02-RESEARCH.md` §2（.entry.render() + <Content /> 唯一干净路径）+ §TOOL-02（详情页完整实现参考：面包屑 / hero / MDX body / 标签 chip 行 / 底部 nav / slug title 中段）+ R-2（同名 slug 冲突 build 期 assert）
- `.planning/phases/02-toolkit-core/02-PATTERNS.md` N-6（完整 [slug].astro skeleton + import 清单 + getStaticPaths 含唯一性 assert + 详情 page）
- `.planning/phases/02-toolkit-core/02-UI-SPEC.md` § Layout & Grid（详情页垂直骨架）+ § Typography（detail h1 28px display）+ § Copywriting（title 中段 `{名} · 工具库 · 初曦的窝`；外链按钮 aria-label；related 区块标题「同分类的其他工具」）
- `src/components/ToolDetailNav.astro`（02-02 新建组件）
- `src/components/TagCloud.astro`（标签 chip 渲染 reuse）
- `src/lib/tools.ts` getAllTools/getRelated/slugOf
- `src/layouts/BaseLayout.astro` fullTitle/title 逻辑
</read_first>
<action>
新建 `src/pages/tools/[slug].astro`。

import: `RenderResult` from 'astro:content'；BaseLayout + Header + Footer + Breadcrumb + TagCloud + ToolDetailNav + `{ Icon }` from 'astro-icon/components' + `{ getAllTools, getRelated, slugOf }` from lib/tools。

**getStaticPaths**（参照 patterns N-6 含 R-2 唯一性 assert）：
```
const tools = await getAllTools();
const slugs = tools.map((t) => slugOf(t.data.title));
if (new Set(slugs).size !== slugs.length) {
  const dupes = slugs.filter((s, i) => slugs.indexOf(s) !== i);
  throw new Error(`[tools] duplicate slug: ${[...new Set(dupes)].join(', ')}`);
}
return tools.map((t) => ({
  params: { slug: slugOf(t.data.title) },
  props: { entry: t },
}));
```

**page frontmatter**：
```
const { entry } = Astro.props;
const { Content } = await entry.render();
const { title, url, summary, tags, category } = entry.data;
const related = await getRelated(category, slugOf(title), 2);
```

**页面结构**：
- `<BaseLayout title={`${title} · 工具库`} description={summary}>` → BaseLayout 自动补 `· 初曦的窝`
- `<Header />`
- `<main class="mx-auto max-w-[72rem] px-4 py-12 md:px-6">`
- `<Breadcrumb items={[{ href: '/', label: '首页' }, { href: '/tools', label: '工具库' }, { label: title }]} />`
- Hero `<header class="mt-8">`：左 h1 28px display + 右外链按钮（**hero 外链按钮与 EntryCard absolute 外链按钮视觉完全一致**：inline-flex min-h-11 min-w-11 rounded-md + Icon ph:arrow-square-out + `href={url}` `target="_blank"` `rel="noopener noreferrer"` `aria-label={'${title} 外链'}`）+ summary p + category p。（**关键不变量 / TOOL-05：hero 外链按钮 `target="_blank"` 那一行必须同行含 `rel="noopener noreferrer"`**，与 EntryCard absolute 外链按钮完全对齐）
- p summary 下 meta 行：`{category}` 13px muted
- TagCloud tags = tags.map(ta => ({ href: `/tools/tag/${ta}`, label: ta }))
- `<article class="prose mt-12"><Content /></article>`
- `<ToolDetailNav backHref="/tools" backLabel="← 返回工具库" category={category} related={related} />`
- `<Footer />`
</action>
<verify>
- `grep -c 'target="_blank"' src/pages/tools/\[slug\].astro` = 1（hero 外链按钮）
- `grep -c 'rel="noopener noreferrer"' src/pages/tools/\[slug\].astro` = 1
- `grep -c 'new Set(slugs)' src/pages/tools/\[slug\].astro` ≥ 1（R-2 唯一性 assert）
- `grep -c "entry.render" src/pages/tools/\[slug\].astro` ≥ 1
- `grep -c "ToolDetailNav" src/pages/tools/\[slug\].astro` ≥ 1
- `npm run build` 通过且 `/tools/${encodeURIComponent(seedTitle)}` 路由生成
</verify>
<acceptance_criteria
`[slug].astro` 详情页：getStaticPaths 含同名 title 唯一性 assert；`entry.render()` + `<Content />` MDX 渲染；面包屑 / hero 外链按钮（noopener）/ summary / meta / TagCloud / ToolDetailNav 完整链路；build 通过后 `http://localhost:4321/tools/${encodeURIComponent(seedTitle)}` 可访问并见到 MDX 渲染结果 + 底部 nav（不足 2 个 related 时整区 hidden）。
</acceptance_criteria>
</task>

<task id="02-03-T3">
<name>分类档案 category/[category].astro</name>
<files>src/pages/tools/category/[category].astro</files>
<read_first>
- `.planning/phases/02-toolkit-core/02-CONTEXT.md` §decisions D-05（分类走静态页）+ D-08（复用主页网格 + BaseLayout + 替换数据集/标题/面包屑/空状态）
- `.planning/phases/02-toolkit-core/02-RESEARCH.md` §TOOL-03（分类档案完整实现 + pill active 态判定）+ §1 getStaticPaths pattern
- `.planning/phases/02-toolkit-core/02-PATTERNS.md` N-7（完整分类档案 skeleton）
- `.planning/phases/02-toolkit-core/02-UI-SPEC.md` § Copywriting（分类档空状态 heading `这个分类还没住进任何工具 (｡•́︿•̀｡)` + CTA `返回工具库`；档案页 Heading 24px display `分类：{category}` + meta `（{N} 个工具）`）
- `src/lib/tools.ts` getToolsByCategory / slugOf
</read_first>
<action>
新建 `src/pages/tools/category/[category].astro`。

import: BaseLayout + Header + Footer + EntryCard + Breadcrumb + EmptyState + PillRow + `{ getAllTools, slugOf }` from ../../../lib/tools（路径深度 ../../../ 不同于 [slug] 的 ../../../ — 注意双层目录）。

getStaticPaths：
```
const tools = await getAllTools();
const cats = [...new Set(tools.map((t) => t.data.category))];
return cats.map((category) => ({
  params: { category },
  props: { items: tools.filter((t) => t.data.category === category), category, totalCategories: cats },
}));
```

page：
```
const { items, category, totalCategories } = Astro.props;
```
`<BaseLayout title={'分类：${category}'} description={'分类「${category}」下的全部工具'}>` → Header + main：

面包屑 items=[{ href: '/', label: '首页' }, { href: '/tools', label: '工具库' }, { label: '分类：${category}' }]；

pill row 由当前 url 判定 active：`PillRow class="mt-8" pills={[ { href: '/tools', label: '全部工具', active: Astro.url.pathname === '/tools' }, ...totalCategories.map((c) => ({ href: '/tools/category/${c}', label: c, active: c === category })) ]}`；

h1 `分类：{category}` + p meta `（{items.length} 个工具）`；

空状态分支 items.length === 0 ? `<EmptyState heading="这个分类还没住进任何工具 (｡•́︿•̀｡)" cta="/tools" ctaLabel="返回工具库" />` 否则 `<section class="mt-8 grid grid-cols-1 gap-6 md:grid-cols-2 md:gap-6 lg:grid-cols-3 lg:gap-8">` + items.map EntryCard（href/externalUrl/icon=cog → ph:wrench/title/summary）；

Footer 前 `mt-24`；Footer。Cog 工具 icon：EntryCard 沿用 `ph:wrench`。
</action>
<verify>
- `grep -c "items.filter\|category" src/pages/tools/category/\[category\].astro` ≥ 1
- `grep -c 'aria-current' src/pages/tools/category/\[category\].astro` ≥ 1（pill active）
- `grep -c "这个分类还没住进任何工具" src/pages/tools/category/\[category\].astro` ≥ 1
- `npm run build` 通过且 `/tools/category/${encodeURIComponent(seedCategory)}` 生成
</verify>
<acceptance_criteria
`category/[category].astro` 分类静态档案：getStaticPaths 枚举 categories + filter 同名；复用 homepage grid + PillRow active 态 + Breadcrumb + EmptyState；空状态 heading CTA 文案与 UI-SPEC 锁定一致；build 通过且 `/tools/category/效率工具`（举例）着陆后只列出该分类工具。
</acceptance_criteria>
</task>

<task id="02-03-T4">
<name>标签档案 tag/[tag].astro</name>
<files>src/pages/tools/tag/[tag].astro</files>
<read_first>
- `.planning/phases/02-toolkit-core/02-CONTEXT.md` §decisions D-06（标签静态页 + 不归一也要去重；多 tag 多档案）+ D-08（复用主页网格）
- `.planning/phases/02-toolkit-core/02-RESEARCH.md` §TOOL-04（标签档案完整实现参考 + tags transform 归一化聚合 + 标签 chip 排序按 count 降序；`#` aria-hidden）+ §1 getStaticPaths Map 写法
- `.planning/phases/02-toolkit-core/02-PATTERNS.md` N-8（完整标签档案 skeleton + tagMap 聚合）
- `.planning/phases/02-toolkit-core/02-UI-SPEC.md` § Copywriting（标签档空状态 heading `还没有工具打过这个标签～` + CTA `返回工具库`；标签 chip `#` aria-hidden；命中区 ≥32px）
- `src/lib/tools.ts` getAllTools / slugOf + T-1 transform 归一化
- `src/components/TagCloud.astro` 02-02 T3
</read_first>
<action>
新建 `src/pages/tools/tag/[tag].astro`。

import: BaseLayout + Header + Footer + EntryCard + Breadcrumb + EmptyState + TagCloud + `{ getAllTools, slugOf }` from ../../../lib/tools（双层目录）。

getStaticPaths（参照 RESEARCH §1 tagMap 聚合写法，tags 已经 02-01 transform 归一化）：
```
const tools = await getAllTools();
const tagMap = new Map();
for (const t of tools) for (const tag of t.data.tags) {
  const list = tagMap.get(tag) ?? [];
  list.push(t);
  tagMap.set(tag, list);
}
return [...tagMap.entries()].map(([tag, items]) => ({
  params: { tag },
  props: { items, tag },
}));
```

page + 全量 chips（含当前 tag active 标识）需再跑一个 getAllTools + tagCounts Map（与 homepage 同算法）：
```
const tools = await getAllTools();
const tagCounts = new Map();
for (const t of tools) for (const tag of t.data.tags) tagCounts.set(tag, (tagCounts.get(tag) ?? 0) + 1);
const tags = [...tagCounts.entries()].sort((a,b) => b[1]-a[1]).map(([ta]) => ta);
```

`<BaseLayout title={'标签：${tag}'} description={'打过「${tag}」标签的全部工具'}>` → Header + main：

面包屑 items=[{ href: '/', label: '首页' }, { href: '/tools', label: '工具库' }, { label: '标签：${tag}' }]；

TagCloud class="mt-8" tags = tags.map(ta => ({ href: '/tools/tag/${ta}', label: ta, count: tagCounts.get(ta), active: ta === tag }))；

h1 `标签：{tag}` + p meta `（{items.length} 个工具）`；

items.length === 0 ? `<EmptyState heading="还没有工具打过这个标签～" cta="/tools" ctaLabel="返回工具库" />` 否则 grid + items.map EntryCard（同 category 档案）。

Footer + `mt-24`。
</action>
<verify>
- `grep -c "tagMap\|new Map" src/pages/tools/tag/\[tag\].astro` ≥ 1
- `grep -c "还没有工具打过这个标签" src/pages/tools/tag/\[tag\].astro` ≥ 1
- `grep -c 'aria-current' src/pages/tools/tag/\[tag\].astro` ≥ 1（TagCloud 内 chip active）
- `npm run build` 通过且 `/tools/tag/macos`（举例）访问列出 raycast
</verify>
<acceptance_criteria
`tag/[tag].astro` 标签静态档案：getStaticPaths 经 getAllTools + Map 聚合生成归一化标签路径；复用 homepage grid + TagCloud active 态 + Breadcrumb + EmptyState；空状态 heading CTA 文案与 UI-SPEC 锁定一致；build 通过后 `/tools/tag/macos` 着陆列出 raycast。
</acceptance_criteria>
</task>

<task id="02-03-T5">
<name>build + check + V-1..V-8 验收 + 推送 main</name>
<files>(meta 任务：无新文件；跑命令验证计划产物)</files>
<read_first>
- `.planning/phases/02-toolkit-core/02-RESEARCH.md` § Validation Architecture 表 V-1..V-8（完整验收标准）
- `.planning/phases/02-toolkit-core/02-PATTERNS.md` §4 强制验收 grep 脚本（V-5 外链 rel 不变量 + R-3 禁止嵌套 <a>）
- `.planning/phases/02-toolkit-core/02-CONTEXT.md` §code_context Integration Points（外链 rel 强制 / anime 不动点）
- `src/` 全部新落位文件（前 4 个任务产物）
</read_first>
<action>
执行 Phase 2 集成验收脚本，全部通过后再推送 main：

```
npx astro sync && npx astro check          # V-6 anime 零 schema warning + TS 双校验
npm run build                               # 触发 sitemap V-8 全 /tools/** 路由收录

# V-5 外链 rel 不变量（本 plan 新增路径内 target_blank 与 rel 数量须一致）
SRC=$(grep -rl 'target="_blank"' src/ | wc -l); REL=$(grep -rl 'rel="noopener noreferrer"' src/ | wc -l); echo "target=$SRC rel=$REL"; [ "$SRC" = "$REL" ] || echo "MISMATCH: 存在未配 rel 的外链"
```
 expectations：SRC == REL（目前 2 — EntryCard absolute 外链按钮 + [slug] hero 外链按钮，数量均为 2；其它引用的文件不含 target_blank）

```
# R-3 禁止嵌套 <a>（ast check + 肉眼）
grep -n '<a [href\|class][^>]*>' src/components/EntryCard.astro | head
```

```
# V-8 sitemap 全收录
cat dist/sitemap-0.xml | grep -E '/tools' | head
```

```
# V-6 anime 零改动
git diff --stat HEAD -- src/content.config.ts src/lib/anime.ts   # anime.ts 不应出现
```

本地 dev 服务器目视验证：
- `http://localhost:4321/tools` 主页渲染 + summary + pill row + tag cloud + 网格（V-1）
- 点击任一卡 进 /tools/{slug}，MDX 正文渲染 + 底部 nav（V-2）
- 点分类 pill 进 /tools/category/{x} + pill active 态 aria-current 被朗读（V-3）
- 写 seed tag 混用 JS/js/空格，断言归一后 `/tools/tag/js` 页含所有该 key 工具（V-4）
- 三断点 + 暗色 + 无 FOUC（V-7，沿用 Phase 1 验收方式）

全部通过后：`git add -A && git commit -m "feat(02): 工具库核心 主页 + 详情 + 分类档案 + 标签档案" && git push origin main`（走 Vercel 自动构建部署，STATE 已记代理 `127.0.0.1:7897`）。**.  .
</action>
<verify>
- `npx astro check` 零 error
- `npm run build` 通过
- `$SRC == $REL`（target=_blank 与 rel=noopener 数量完全对齐）
- `dist/sitemap-0.xml` 含 `/tools` + `/tools/category/...` + `/tools/tag/...` + `/tools/${seed slug}`
- `git diff --stat` 输出不含 `src/lib/anime.ts` 与 `src/content.config.ts` 中 anime block
- dev 服务器访问 /tools 渲染 OK + 跳转详情 OK + 分类 pill active 可读 + 标签云可用
- push 后 Vercel 生产部署 https://chuxisite.vercel.app 可访问
</verify>
<acceptance_criteria>
02-03 plan 验收全部通过：astro check 零 error；build 通过；`target=_blank` 与 `rel="noopener noreferrer"` 数量完全对齐；sitemap 含 /tools + 详情 + 分类 + 标签全路由；anime.ts schema/content.config.ts anime block 零改动；dev + 目视验证 V-1/V-2/V-3/V-4/V-7 通过；推送到 main 后 Vercel 生产部署 https://chuxisite.vercel.app 可访问新页面。
</acceptance_criteria>
</task>

## must_haves

```yaml
must_haves:
  truths:
    - "访客访问 /tools 看到卡片网格列出所有 no-draft 工具（名/summary/外链按钮/tags chip）"
    - "访客点击卡片进入 /tools/{slug} 独立 MDX 详情页；底部 nav 「← 返回工具库」+ 同分类 1-2 卡片"
    - "访客可进入分类档案 /tools/category/{x} 且仅列出该分类工具；pill aria-current 标识激活"
    - "访客可进入标签档案 /tools/tag/{t} 且仅列出该标签工具；tags 归一化后分裂标签合流"
    - "所有外链 target=_blank + rel=noopener noreferrer + aria-label"
    - "sitemap 自动收录全部 /tools/** 路由；anime 集合零改动；主题 / FOUC / 暗色继承 Phase 1"
  artifacts:
    - path: "src/pages/tools/index.astro"
      provides: "完整工具库主页（summary + pills + cloud + grid）"
      contains: "共.*件工具.*慢慢整理中"
    - path: "src/pages/tools/[slug].astro"
      provides: "MDX 详情页 + breadcrumb + 外链按钮 + 底部 nav"
      contains: "entry.render"
    - path: "src/pages/tools/category/[category].astro"
      provides: "分类档案静态页"
      contains: "getStaticPaths"
    - path: "src/pages/tools/tag/[tag].astro"
      provides: "标签档案静态页"
      contains: "new Map"
  key_links:
    - from: "src/pages/tools/index.astro"
      to: "src/lib/tools.ts"
      via: "getAllTools"
      pattern: "getAllTools"
    - from: "src/pages/tools/[slug].astro"
      to: "src/lib/tools.ts"
      via: "getAllTools + getRelated + slugOf"
    - from: "src/pages/tools/category/[category].astro"
      to: "src/lib/tools.ts"
      via: "getAllTools + slugOf"
    - from: "src/pages/tools/tag/[tag].astro"
      to: "src/lib/tools.ts"
      via: "getAllTools + slugOf"

must_not_touch:
  - "src/content.config.ts 的 anime block（bgmId/status/myRating 等完整保留）"
  - "src/lib/anime.ts（Phase 3 Bangumi 预留不动点）"
  - "src/components/BaseLayout.astro FOUC head 脚本"
  - "src/styles/global.css @theme / :where(.dark) / :focus-visible baseline"

validation:
  v1: "build 后 grep 构建产物 HTML 卡片数 = getAllTools().length"
  v2: "点击卡片跳转 /tools/{slug} 看到 <Content /> 渲染 MDX + 底部 nav"
  v3: "分类 pill href 着陆档案页；数据集长度 = getToolsByCategory 理论数；aria-current 朗读"
  v4: "写 seed tag 混用 JS/js/空格，断言归一后统一 /tools/tag/js 含所有该 key 工具"
  v5: "grep target=_blank 数量 == grep rel=noopener noreferrer 数量"
  v6: "git diff --stat 不含 anime.ts 与 content.config.ts anime block"
  v7: "三断点 1/2/3 列 + 暗色 + 无 FOUC"
  v8: "dist/sitemap-0.xml 含 /tools 全路由；title 拼接正确"

references:
  research_sections: ["§1", "§2", "§TOOL-01", "§TOOL-02", "§TOOL-03", "§TOOL-04", "§TOOL-05", "Validation V-1..V-8"]
  context_decisions: ["D-01", "D-02", "D-03", "D-05", "D-06", "D-08", "D-09", "D-10", "D-11", "D-12", "D-13", "D-14"]
  patterns: ["M-4", "N-6", "N-7", "N-8"]
  ui_spec_pages: ["Layout & Grid 主页/详情/档案三骨架", "Component Inventory EntryCard + pills + tag cloud + breadcrumb + empty state + detail nav + MDX 正文区", "Copywriting Contract 全套文案", "Spacing / Typography / Color / Registry Safety", "Requirement Traceability TOOL-01..05"]

next_plan: "本阶段最后 plan；完成后 Phase 2 全部交付 → 可启动 Phase 3（追番记录）"

---
phase: 01-foundation-shell
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/styles/global.css
  - public/fonts/zcool-subset.woff2
  - src/components/Header.astro
  - src/components/ThemeToggle.astro
  - src/components/Footer.astro
  - src/components/deco/Star.astro
  - src/components/deco/Heart.astro
  - src/components/deco/Blob.astro
  - src/components/deco/Divider.astro
  - src/components/Hero.astro
  - src/components/IntroBlock.astro
  - src/components/EntryCard.astro
  - src/pages/index.astro
autonomous: true
requirements: [INFRA-03, INFRA-05, INFRA-07]

must_haves:
  truths:
    - "全站呈现鲜艳可爱二次元视觉：青蓝主色、圆润大圆角、软萌阴影，亮/暗双主题令牌生效"
    - "标题/Logo 用装饰字体（Fredoka + ZCOOL KuaiLe 子集），正文用系统栈；无 >1MB 字体请求"
    - "首页含 Hero（标语「工具、番剧、和一点点生活」+ 主CTA 进入工具库→/tools + 次CTA 查看追番→/anime + 插画占位 blob 回退）、IntroBlock、2× 整卡链接 EntryCard"
    - "两入口卡 href 分别为 /tools、/anime，1 列（<md）→ 2 列（≥md），hover 上浮包裹于 prefers-reduced-motion guard"
    - "Header sticky+blur、ThemeToggle 44×44 可切换主题、Footer 软萌签名 + 手绘 divider；装饰元素 aria-hidden + pointer-events-none"
    - "响应式无横向溢出，触控命中区 ≥44px"
  artifacts:
    - path: "src/styles/global.css"
      provides: "@theme 令牌（亮/暗配色、圆角、shadow-soft、font-display/body）+ @custom-variant dark + 字体加载"
      contains: "--color-primary"
    - path: "src/components/EntryCard.astro"
      provides: "整卡 <a> 入口卡（图标圆底+标题+描述+箭头）"
      contains: "href"
    - path: "src/pages/index.astro"
      provides: "首页落地页：Header + Hero + IntroBlock + 2×EntryCard + Footer"
      contains: "工具、番剧、和一点点生活"
    - path: "public/fonts/zcool-subset.woff2"
      provides: "ZCOOL KuaiLe 标题用字子集（<30KB）"
  key_links:
    - from: "src/pages/index.astro"
      to: "/tools 与 /anime"
      via: "EntryCard href + Hero CTA"
      pattern: "href=\"/(tools|anime)\""
    - from: "src/pages/index.astro"
      to: "src/components/Hero.astro"
      via: "组件组装"
      pattern: "Hero"
---

<objective>
交付二次元视觉外壳与首页落地页：Tailwind v4 `@theme` 令牌（亮/暗配色、圆角、阴影、装饰字体）、Header/Nav/ThemeToggle/Footer 与装饰 SVG、首页 Hero + IntroBlock + 2× 入口卡，全站响应式。所有取值以 01-UI-SPEC.md 为唯一契约。

Purpose: 满足 INFRA-03（响应式）、INFRA-05（鲜艳可爱二次元视觉基线）、INFRA-07（落地首页 + 简介 + 两入口）。
Output: global.css 令牌 + 外壳组件 + 首页。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-shell/01-UI-SPEC.md
@.planning/phases/01-foundation-shell/01-CONTEXT.md
@.planning/phases/01-foundation-shell/01-RESEARCH.md

<interfaces>
BaseLayout（01-01 创建）已含 `<head>` 内联主题脚本、字体 preload 位、`slot`。本计划不改 BaseLayout：首页 index.astro 用 BaseLayout 并在其 slot 内组装 Header/Hero/IntroBlock/EntryCard/Footer；这些组件供 01-03 占位页/404 复用。`astro add tailwind` 已在 global.css 写入 `@import "tailwindcss"`；本计划在其后追加 `@custom-variant`、`@theme` 与字体。图标用 `astro-icon` 的 `Icon`（`ph:` 前缀）。字体 preload 的 `<link>` 由 01-01 BaseLayout head 承载，本计划只产出字体文件与 @font-face。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: global.css @theme 令牌 + 暗色变体 + 字体加载</name>
  <files>src/styles/global.css, public/fonts/zcool-subset.woff2</files>
  <read_first>src/styles/global.css（01-01 已有 @import）；01-UI-SPEC.md「Color」全量调色板表、「Radius」、「Typography」字体 token；01-RESEARCH.md「Tailwind v4 @theme Token 设置」「字体加载」</read_first>
  <action>在 `@import "tailwindcss"` 后追加 `@custom-variant dark (&:where(.dark, .dark *))`。写 `@theme`：font-display=`"Fredoka Variable","ZCOOL KuaiLe",system-ui,sans-serif`、font-body=系统栈；radius sm/md/lg/xl=10/16/24/32px、full=9999px；`--shadow-soft: 0 6px 24px -8px rgba(23,182,214,0.25)`；亮色全量 `--color-*` 逐一按 UI-SPEC Color 表精确 hex（bg #F3FBFE、surface #FFFFFF、surface-muted #E7F6FB、primary #17B6D6、primary-hover #0FA0BE、primary-fg #FFFFFF、link #0A7C93、text #0F2E3A、text-muted #5A7A85、border #CDEBF3、success #34C77B、destructive #F26D6D、deco-pink #FF8FC7）。暗色不写第二套 @theme，而在 `:where(.dark)` 作用域重定义同名变量（bg #0C1B22、surface #12262F、surface-muted #17303A、primary #3AD2EF、primary-hover #5CDCF2、primary-fg #04222B、link #6FE0F5、text #E6F6FA、text-muted #94B4BF、border #234450、deco-pink #FF9ECF、shadow-soft 透明度降至 0.15）。`@import '@fontsource-variable/fredoka';`。ZCOOL：先 `ls node_modules/@fontsource/zcool-kuaile/files` 确认实际 woff2 名（A4），用 pyftsubset `--text="初曦的窝工具库追番记录进入查看和一点点生活这里还空空如也诶呀一页走丢了"` 生成 public/fonts/zcool-subset.woff2，写 `@font-face{font-family:"ZCOOL KuaiLe";src:url('/fonts/zcool-subset.woff2') format('woff2');font-display:swap}`。若 pyftsubset 不可用则 `pip install fonttools brotli`；仍失败则仅上 Fredoka 并在 SUMMARY 记 ZCOOL 延后。设 body 基线：`font-family:var(--font-body)`、`background:var(--color-bg)`、`color:var(--color-text)`；`:focus-visible` 2px primary 焦点环 + 2px offset。</action>
  <verify>
    <automated>npm run build && grep -q -- '--color-primary: #17B6D6' src/styles/global.css && grep -q 'custom-variant dark' src/styles/global.css</automated>
  </verify>
  <acceptance_criteria>
    - `global.css contains --color-primary: #17B6D6`
    - `global.css contains @custom-variant dark`（class 策略）
    - 暗色在 `:where(.dark)` 重定义 --color-bg 等同名变量
    - radius sm/md/lg/xl = 10/16/24/32px 且 --shadow-soft 存在
    - Fredoka 通过 @fontsource import；ZCOOL 走 /fonts/zcool-subset.woff2（若延后须在 SUMMARY 记录）
    - 无单个 >1MB 字体请求（P5）
  </acceptance_criteria>
  <done>@theme 令牌与暗色变体生效，工具类（bg-bg/text-text/border-border/shadow-soft）可用；标题字体就绪，正文系统栈。</done>
</task>

<task type="auto">
  <name>Task 2: Header/Logo/ThemeToggle/Footer + 装饰 SVG 组件</name>
  <files>src/components/Header.astro, src/components/ThemeToggle.astro, src/components/Footer.astro, src/components/deco/Star.astro, src/components/deco/Heart.astro, src/components/deco/Divider.astro</files>
  <read_first>01-UI-SPEC.md「Layout & Component Inventory」Header/Logo/ThemeToggle/Footer/Decoration 行、「Motion & Accessibility」；01-RESEARCH.md「主题系统实现②」「装饰 SVG」「图标」</read_first>
  <action>Header.astro：sticky top-0、`bg-surface`、`backdrop-blur`、底部 `border-border`；含 Logo（`--font-display` 站名「初曦的窝」+ ✦ 星星 `aria-hidden`）、导航链接 工具库→/tools、追番→/anime（命中区 ≥44px，激活态主色下划线/圆点，用 `Astro.url.pathname` 判定 active）、ThemeToggle。ThemeToggle.astro：`rounded-full` 44×44 图标按钮，太阳/月亮（`ph:sun`/`ph:moon`，装饰图标 aria-hidden），`aria-label="切换深浅色主题"`；普通 `<script>`（非 is:inline）绑定 click：toggle `document.documentElement.classList.toggle('dark')` 并写 `localStorage.theme`。Footer.astro：`bg-surface`、muted 版权 + 一句软萌签名，顶部用 Divider 手绘 SVG。deco/Star、Heart：内联 SVG 组件，`aria-hidden="true"`、容器 `pointer-events-none absolute`、`--color-deco-pink` 填充。deco/Divider：手绘波浪分隔 SVG，`aria-hidden`。所有 hover/过渡仅 transform/opacity/color ≤200ms，非必要动效包 `@media (prefers-reduced-motion: reduce)` 关闭。</action>
  <verify>
    <automated>npm run build && grep -q '切换深浅色主题' dist/index.html && grep -q 'aria-hidden' dist/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `ThemeToggle contains aria-label="切换深浅色主题"` 且按钮 44×44 rounded-full
    - Header sticky + backdrop-blur + border-border；导航链接 href /tools 与 /anime，命中区 ≥44px
    - Logo 含站名「初曦的窝」+ ✦ 且 ✦ aria-hidden
    - `decorative svgs have aria-hidden="true"` 且容器 pointer-events-none
    - 点击 ThemeToggle 切换 .dark 并持久化 localStorage.theme
  </acceptance_criteria>
  <done>Header/ThemeToggle/Footer 与装饰 SVG 就绪，主题可切换并记忆，装饰不入 Tab 序、不拦点击。</done>
</task>

</tasks>

<task type="auto">
  <name>Task 3: Hero + IntroBlock + 插画占位 blob</name>
  <files>src/components/Hero.astro, src/components/IntroBlock.astro, src/components/deco/Blob.astro</files>
  <read_first>01-UI-SPEC.md「Hero 规格」「Typography」Display 行、「Copywriting Contract」Primary/Secondary CTA；01-CONTEXT.md D-03 标语；01-RESEARCH.md 占位 blob</read_first>
  <action>Hero.astro：≥lg 两栏（左文字/右插画），<lg 单栏堆叠、插画在文字下方。左栏 Display 主标题（--font-display，clamp(32px,6vw,52px)/600/lh1.15）= 标语「工具、番剧、和一点点生活」（D-03）+ 副标题 Body/muted + 主 CTA `进入工具库`→/tools（主色填充+primary-fg 白字，radius-md）+ 次 CTA `查看追番`→/anime（边框次要样式）。右栏走占位回退 deco/Blob.astro：max-width:520px、固定 aspect-ratio:4/3、--radius-xl、primary→deco-pink 青粉渐变、中心 1–2 Star/Heart、aria-hidden、pointer-events-none、轻浮动动效包 prefers-reduced-motion guard，占位无实义文本。Hero 背景 primary→deco-pink 低透明渐变 + 少量漂浮星点（aria-hidden）。本阶段用占位 blob 代 <Image>（种子无图），保证 CLS≈0。IntroBlock.astro：Hero 下 2xl 上间距，Heading（display）+ 1–2 句站点简介 Body，居中。</action>
  <verify>
    <automated>npm run build && grep -q '工具、番剧、和一点点生活' dist/index.html && grep -q 'href="/tools"' dist/index.html && grep -q 'href="/anime"' dist/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `home HTML contains 工具、番剧、和一点点生活`（Display 标题，font-display）
    - Hero 主 CTA href="/tools"、次 CTA href="/anime"
    - 占位 blob 固定 aspect-ratio 4/3 + radius-xl、青粉渐变、aria-hidden + pointer-events-none
    - `hover/浮动 transform inside prefers-reduced-motion guard`
    - IntroBlock 渲染站点简介 Heading + Body
  </acceptance_criteria>
  <done>Hero 呈现标语+双 CTA+插画占位（无图优雅回退、CLS≈0），下接站点简介。</done>
</task>

<task type="auto">
  <name>Task 4: EntryCard + 首页组装 + 响应式核验</name>
  <files>src/components/EntryCard.astro, src/pages/index.astro</files>
  <read_first>01-UI-SPEC.md「EntryCard」契约、「Copywriting Contract」入口卡文案、「Layout」容器/断点；01-CONTEXT.md D-07；01-RESEARCH.md「首页组成」</read_first>
  <action>EntryCard.astro：props href/title/description/icon/emphasis?。整卡为单个 `<a href>`（可点区=整卡，内部链接无需 rel），结构：图标圆底（--radius-full，primary 10% 染底 + ph 图标）+ 标题（Heading，display）+ 描述（Body/muted）+ 箭头（ph:arrow-right）。--radius-xl + shadow-soft；hover translateY(-2px) ≤180ms 包 @media (prefers-reduced-motion: reduce) 关闭；emphasis 卡（工具库）视觉略重：主色描边/更强染底。index.astro：用 BaseLayout（传首页特化 title `初曦的窝 · 工具、番剧、和一点点生活` D-03 + description），slot 内自上而下：Header → Hero → IntroBlock → EntryCard×2（容器最大宽 72rem 居中、左右安全内距 md→lg；`grid gap-[32px]` 1 列、`md:grid-cols-2`；工具库卡 emphasis、href=/tools、标题「工具库」描述「我收藏与折腾过的好用软件 / 网站，分类整理、图文记录」；追番卡 href=/anime、标题「追番记录」描述「在看 / 看完 / 想看的番，评分、进度与短评都在这里」）→ Footer。构建后目视 <md 单列、≥md 双列，移动/桌面无横向溢出。</action>
  <verify>
    <automated>npm run build && grep -q 'href="/tools"' dist/index.html && grep -q 'href="/anime"' dist/index.html && grep -q '追番记录' dist/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `EntryCard hrefs = /tools and /anime`（整卡 <a>，可点区=整卡）
    - 两卡文案匹配 UI-SPEC（工具库 / 追番记录 标题与描述）
    - 1 列（<md）→ 2 列（md:grid-cols-2），gap 32px，工具库卡 emphasis 略重
    - `hover transform inside prefers-reduced-motion guard`
    - 首页 `<title>` 为「初曦的窝 · 工具、番剧、和一点点生活」；容器最大宽 72rem 居中，无横向溢出
  </acceptance_criteria>
  <done>首页完整组装，两入口卡整卡可点导向 /tools /anime，响应式单列↔双列切换无错位。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 无 | 纯静态只读外壳，无用户输入、无表单、无后端、无密钥 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Tampering | Hero CTA / EntryCard 链接 | accept | 全为内部相对链接 `/tools` `/anime` `/`，无外链，无需 rel |
| T-01-02-02 | Tampering | 未来外链（若加）| mitigate | 任何 `target="_blank"` 外链强制 `rel="noopener noreferrer"`（本阶段无，规则预置） |
| T-01-02-03 | Information Disclosure | ThemeToggle localStorage | accept | 仅存主题偏好字符串，无敏感信息 |

无 high 级需阻断项；本计划无 npm 安装（依赖 01-01 已装包）、无用户输入、无 secret。
</threat_model>

<verification>
- `npm run build` 成功，dist/index.html 产出且无横向溢出。
- global.css 含全量 @theme 令牌（--color-primary #17B6D6 等）+ @custom-variant dark + 暗色 :where(.dark) 覆盖。
- 首页含标语「工具、番剧、和一点点生活」、主/次 CTA 指向 /tools /anime、2× EntryCard 整卡链接 /tools /anime、文案逐字匹配 UI-SPEC。
- Header sticky+blur、ThemeToggle 44×44 可切换并记忆、Footer 软萌签名 + 手绘 divider。
- 装饰元素（Logo ✦、Star/Heart/Blob/Divider）aria-hidden + pointer-events-none；触控命中区 ≥44px。
- 无单个 >1MB 字体请求（P5）；hover/浮动动效均在 prefers-reduced-motion guard 内。
</verification>

<success_criteria>
INFRA-05 达成：Tailwind v4 @theme 令牌（亮/暗配色、圆角、阴影、装饰字体）落地，鲜艳可爱二次元视觉基线成立。
INFRA-07 达成：首页 Hero（标语 + 双 CTA + 插画占位回退）+ IntroBlock 简介 + 2× 可点入口卡（→/tools /anime）。
INFRA-03 达成：全站响应式，1 列（<md）↔ 2 列（≥md）无错位、无横向溢出、触控 ≥44px。
</success_criteria>

<output>
Create `.planning/phases/01-foundation-shell/01-02-SUMMARY.md` when done
</output>

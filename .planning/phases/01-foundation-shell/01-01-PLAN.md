---
phase: 01-foundation-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - astro.config.mjs
  - .gitattributes
  - src/content.config.ts
  - src/content/tools/raycast.mdx
  - src/content/anime/frieren.mdx
  - src/lib/tools.ts
  - src/lib/anime.ts
  - src/styles/global.css
  - src/layouts/BaseLayout.astro
  - src/components/ThemeToggle.astro
  - src/pages/index.astro
autonomous: false
requirements: [INFRA-01, INFRA-02, INFRA-04]

must_haves:
  truths:
    - "访客打开首页见站名「初曦的窝」与种子内容标题（经 Zod schema 校验后渲染）"
    - "亮/暗主题切换按钮工作：翻 .dark class + 写 localStorage，刷新后主题保留"
    - "首屏无 FOUC：<head> is:inline 阻塞脚本在首次绘制前定主题，暗色刷新无闪白"
    - "改内容/配置文件 + git push 触发 Vercel 自动构建（schema 构建期校验）+ CDN 上线"
    - "npm run build 退出 0，tools 与 anime 两集合 schema 校验通过"
  artifacts:
    - path: "astro.config.mjs"
      provides: "site 占位 + image.remotePatterns(lain.bgm.tv) + mdx/sitemap/icon 集成 + tailwind vite 插件"
      contains: "remotePatterns"
    - path: "src/content.config.ts"
      provides: "tools + anime 分层 schema（唯一数据契约）"
      contains: "bgmId"
    - path: "src/lib/tools.ts"
      provides: "getAllTools() 薄封装（过滤 draft + 排序）"
      exports: ["getAllTools"]
    - path: "src/lib/anime.ts"
      provides: "getAllAnime() 薄封装（过滤 draft + 按 status 排序）"
      exports: ["getAllAnime"]
    - path: "src/layouts/BaseLayout.astro"
      provides: "head is:inline 防 FOUC 主题脚本 + slot + footer"
      contains: "is:inline"
    - path: "src/pages/index.astro"
      provides: "最小首页渲染站名 + 种子标题 + ThemeToggle"
      contains: "初曦的窝"
  key_links:
    - from: "src/pages/index.astro"
      to: "src/lib/tools.ts"
      via: "getAllTools 调用（页面不直接 getCollection）"
      pattern: "getAllTools|getAllAnime"
    - from: "src/lib/tools.ts"
      to: "astro:content"
      via: "getCollection('tools')"
      pattern: "getCollection"
    - from: "src/layouts/BaseLayout.astro"
      to: "localStorage theme"
      via: "head is:inline 脚本 + ThemeToggle 原生 script"
      pattern: "localStorage"
---

<objective>
Walking Skeleton：最薄端到端切片。脚手架 Astro 7 + Tailwind v4 + MDX + sitemap + astro-icon + 字体；建立两集合分层数据契约（content.config.ts）与 lib 薄数据层；预置种子内容并经 Zod 校验渲染；主题系统（防 FOUC + localStorage 记忆）；最小首页；部署 Vercel 并回填 site。打通「改文件 → git push → 构建校验 → CDN 上线 → 浏览器渲染 + 主题交互」全链路。

Purpose: 满足 INFRA-01（静态架构 + git 自动部署）、INFRA-02（两集合类型安全数据模型）、INFRA-04（亮/暗切换 + 记忆 + 无 FOUC），并为 01-02（视觉外壳）、01-03（占位页 + SEO）提供地基。
Output: 可构建、可部署、可切主题的站点骨架 + 数据契约 + 薄数据层。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-shell/01-UI-SPEC.md
@.planning/phases/01-foundation-shell/01-CONTEXT.md
@.planning/phases/01-foundation-shell/01-RESEARCH.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 脚手架 + 集成 + astro.config + .gitattributes</name>
  <files>package.json, astro.config.mjs, .gitattributes</files>
  <read_first>01-RESEARCH.md「Scaffold Sequence」「astro.config.mjs」；STACK.md「Installation」；01-CONTEXT.md D-04/D-05（Vercel + site 占位）</read_first>
  <action>
  - 在 D:\workspace\site 根就地初始化（保留 CLAUDE.md/.planning）：`npm create astro@latest . -- --template minimal --typescript strict --no-install --no-git --skip-houston --yes`。flag 语义有出入时回退交互式选 Empty + Strict + 不装依赖/不 git。
  - `npm install` 后 `npx astro add tailwind mdx sitemap --yes`（自动改 astro.config + 装依赖 + 注入 tailwind vite 插件 + 写 @import "tailwindcss"）。
  - `npm install astro-icon @iconify-json/ph @fontsource-variable/fredoka @fontsource/zcool-kuaile`（版本随 latest，均已过 slopcheck，见 RESEARCH Package Legitimacy Audit）。
  - 本阶段明确 NOT 安装 preact/nanostores/pagefind/@astrojs/vercel（无有状态岛屿，P8）。
  - 手改 astro.config.mjs：`site: 'https://chuxi-wo.vercel.app'`（占位，含 TODO 首次部署后回填注释，per D-05）；integrations 加 `icon()`（astro add 不含 astro-icon）；`image.remotePatterns` 加 `{ protocol:'https', hostname:'lain.bgm.tv' }`（Bangumi 远程封面预铺路，本阶段未用）；确认 `vite.plugins` 含 tailwindcss()。output 默认 static，免适配器。
  - 建 .gitattributes（`* text=auto eol=lf`）统一 LF，避免 CRLF diff 噪声（Windows→Vercel）。
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build exits 0`
    - `astro.config.mjs contains remotePatterns` 且 hostname lain.bgm.tv
    - `astro.config.mjs contains icon()` 与 mdx()/sitemap()
    - `astro.config.mjs 的 site 为 https 占位 URL 带回填 TODO 注释`
    - `.gitattributes exists` 含 eol=lf
    - package.json devDependencies 含 astro/@tailwindcss/vite/@astrojs/mdx/@astrojs/sitemap；dependencies 含 astro-icon/@iconify-json/ph/字体包
  </acceptance_criteria>
  <done>脚手架就地建立，集成 + 图标 + 字体装好，astro.config 含 site 占位 + remotePatterns + 三集成，build 绿。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: content.config.ts 两集合 schema + lib 薄数据层 + 种子内容</name>
  <files>src/content.config.ts, src/lib/tools.ts, src/lib/anime.ts, src/content/tools/raycast.mdx, src/content/anime/frieren.mdx</files>
  <read_first>01-RESEARCH.md「content.config.ts」「lib 薄数据层」「种子内容」；STACK.md 两集合结构；01-CONTEXT.md Claude's Discretion（schema 蓝本）</read_first>
  <behavior>
    - tools schema：title/url(url)/summary/tags(string[] default[])/category(string)/cover(image() optional)/draft(bool default false)
    - anime schema 分层：元数据组 bgmId?(number)/titleJa?/titleCn/cover(union[image(),url] optional)/episodes?(number)/airDate?(coerce.date)；状态组 status(enum watching|done|plan)/myRating?(0-10)/progress(default 0)/comment?/draft(default false)
    - 元数据组用 .optional() 不用 .default()（P1）；状态/tags/progress 用 .default()
    - getAllTools() 过滤 !draft，按 title localeCompare('zh') 排序
    - getAllAnime() 过滤 !draft，按 status(watching→plan→done) 排序
    - 种子 frontmatter 全过 schema，astro check 零报错
  </behavior>
  <action>
  - 路径必须 `src/content.config.ts`（Content Layer），非旧 `src/content/config.ts`（P2）。用 `glob({ base:'./src/content/tools', pattern:'**/*.{md,mdx}' })` loader，`schema: ({ image }) => z.object({...})` 函数式签名才拿到 image() helper。anime cover 用 `z.union([image(), z.string().url()])`（image() 放前）。airDate 用 `z.coerce.date()`。导出 `collections = { tools, anime }`。
  - src/lib/tools.ts 导出 getAllTools()；src/lib/anime.ts 导出 getAllAnime()。页面一律经 lib 读取，不直接 getCollection（保番剧换源可插拔，P1/ARCH）。
  - 种子（D-06）：src/content/tools/raycast.mdx（title Raycast / url / summary / tags / category「效率工具」/ draft false，心得写 MDX 正文不进 schema）；src/content/anime/frieren.mdx（titleCn 葬送的芙莉莲 / titleJa / episodes 28 / airDate 2023-09-29 / status done / myRating 9 / progress 28 / comment / draft false；bgmId 留空预留；不带 cover 避免引图）。
  - 建后 `npx astro sync` 生成 astro:content 类型。
  </action>
  <verify>
    <automated>npx astro sync && npx astro check && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/content.config.ts contains bgmId: z.number().optional()`
    - `src/content.config.ts contains status: z.enum(['watching', 'done', 'plan'])`（或等价单引号）
    - `src/content.config.ts 使用 z.union([image(), z.string().url()])`
    - `src/lib/tools.ts exports getAllTools` 且过滤 !draft
    - `src/lib/anime.ts exports getAllAnime`
    - `npx astro check exits 0`（零 frontmatter 报错）
    - `npm run build exits 0`
    - 两种子文件存在且 frontmatter 过 schema
  </acceptance_criteria>
  <done>两集合分层 schema 落定并经 Zod 校验；lib 薄封装收口数据读取；种子内容构建期校验通过。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: global.css @theme + BaseLayout 防 FOUC 主题脚本 + ThemeToggle + 最小首页</name>
  <files>src/styles/global.css, src/layouts/BaseLayout.astro, src/components/ThemeToggle.astro, src/pages/index.astro</files>
  <read_first>01-RESEARCH.md「Tailwind v4 @theme」「主题系统实现」「字体加载」；01-UI-SPEC.md 配色 token + 主题防 FOUC；01-CONTEXT.md D-01（站名）</read_first>
  <behavior>
    - <head> is:inline 脚本在首次绘制前读 localStorage.theme（回退 prefers-color-scheme），dark 则给 documentElement 加 .dark class → 无 FOUC
    - ThemeToggle 点击：documentElement.classList.toggle('dark') + localStorage.setItem('theme', ...)
    - 刷新后主题保留（localStorage 持久）
    - 首页 HTML 含「初曦的窝」与种子标题（Raycast / 葬送的芙莉莲）
  </behavior>
  <action>
  - src/styles/global.css：保留 astro add 写入的 @import "tailwindcss"，追加 @custom-variant dark (&:where(.dark, .dark *))（class 策略非 media，P3）；@theme 定义字体(--font-display/--font-body)、圆角、亮色 --color-* 调色板、--shadow-soft（取值以 01-UI-SPEC 为准，本任务落最小可用子集）；:where(.dark) 作用域重定义同名 --color-* 变量（暗色不在 @theme 写第二套）。Fredoka 走 @import '@fontsource-variable/fredoka/index.css'。ZCOOL KuaiLe 子集化非本任务阻塞项——先只挂 Fredoka + 系统栈，ZCOOL 子集延后到 01-02（RESEARCH 备选/Environment fallback）。
  - src/layouts/BaseLayout.astro：head 首个子元素放 <script is:inline> 主题初始化脚本（原样内联同步执行，绘制前跑）；引入 global.css；提供 slot + 极简 footer；本阶段 props 最小（title 可选，SEO 块由 01-03 追加，勿冲突）。不启用 View Transitions（省 astro:after-swap，RESEARCH Open Q2）。
  - src/components/ThemeToggle.astro：44x44 圆角按钮，aria-label「切换深浅色主题」，用 astro-icon ph:moon/ph:sun（aria-hidden）；普通 <script>（非 is:inline，打包为 module defer）绑 click 切 .dark + 写 localStorage。
  - src/pages/index.astro：用 BaseLayout；经 getAllTools()/getAllAnime() 读种子（不直接 getCollection）；渲染站名「初曦的窝」（D-01）+ 种子标题列表 + ThemeToggle。最小骨架，完整 Hero/落地页属 01-02。
  </action>
  <verify>
    <automated>npm run build && grep -q '初曦的窝' dist/index.html && grep -q 'is:inline' src/layouts/BaseLayout.astro && grep -q 'Raycast' dist/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `<head> contains is:inline theme script`（grep is:inline src/layouts/BaseLayout.astro）
    - `home HTML contains 初曦的窝`（dist/index.html）
    - `dist/index.html 含种子标题 Raycast 与 葬送的芙莉莲`（证 lib→schema→页面管道通）
    - `global.css contains @custom-variant dark` 且 :where(.dark) 重定义 --color-*
    - `src/pages/index.astro 调用 getAllTools`（不直接 getCollection）
    - `localStorage theme persists across reload`（ThemeToggle 写 localStorage.theme，head 脚本读取应用）
  </acceptance_criteria>
  <done>主题系统落地：绘制前定主题无 FOUC + 按钮切换 + localStorage 记忆；最小首页经 lib 渲染站名与种子标题，build 绿。</done>
</task>


<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: 【人工】git 远程 + Vercel 连接 + 首次部署</name>
  <what-built>Task 1-3 已产出可构建的静态站骨架（npm run build 绿）。本地 D:\workspace\site 尚非 git 仓库，Vercel 项目/账号需用户一次性配置（RESEARCH Environment Availability：无 fallback）。</what-built>
  <how-to-verify>
    1. 本地 git init（若未初始化）+ 提交骨架（确认 node_modules/.env 已被 .gitignore 忽略，勿提交密钥）。
    2. 在 GitHub 建远程仓库并 git push。
    3. 登录 Vercel，Import 该 git 仓库；框架自动识别 Astro，output static 免适配器；确认构建成功（Node 22+ 镜像）。
    4. 记下 Vercel 分配的生产域名（xxx.vercel.app）备 Task 5 回填。
  </how-to-verify>
  <resume-signal>粘贴 Vercel 生产域名 URL，或描述部署报错</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: 【人工】回填 site + 首屏无 FOUC 验证</name>
  <what-built>站点已部署到 Vercel 生产域名。需回填真实域名到 astro.config 的 site（sitemap/canonical/OG 依赖，D-05），并人工确认主题记忆无 FOUC（成功标准 2）。</what-built>
  <how-to-verify>
    1. Claude 用 Task 4 提供的真实域名替换 astro.config.mjs 的 site 占位值（删 TODO 注释），git push 触发重建。
    2. 打开生产域名首页：见站名「初曦的窝」与种子标题（Raycast / 葬送的芙莉莲）。
    3. 切到暗色主题后刷新 → 首屏直接暗色、无一瞬白闪（FOUC）；控制台无 hydration 警告。
    4. 再刷新数次确认 localStorage 主题记忆稳定保留。
  </how-to-verify>
  <resume-signal>输入「approved」或描述 FOUC/渲染问题</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 自管内容文件 → 构建 | 唯一「输入」是维护者自写的 .mdx frontmatter，经 Content Layer + Zod 构建期校验 |
| npm 依赖安装 | 脚手架 + 9 个包安装（供应链面） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Tampering | npm install（脚手架 + 9 包） | mitigate | 全 9 包已过 slopcheck（RESEARCH Package Legitimacy Audit，status OK，无 [ASSUMED]/[SUS]/[SLOP]）；提交 package-lock.json 锁定完整性 |
| T-01-01-02 | Information Disclosure | git 提交内容 | mitigate | .gitignore 忽略 node_modules/.env；本阶段无 secret/密钥/CI token（静态站） |
| T-01-01-03 | Tampering | frontmatter 输入 | mitigate | Zod schema 构建期校验；draft 字段 + lib 过滤防私稿误发布 |

无 high 级需阻断项。无 [ASSUMED]/[SUS] 包 → 无需 blocking 合法性 checkpoint。
</threat_model>

<verification>
- `npm run build` 退出 0；`npx astro check` 零 frontmatter 报错。
- astro.config.mjs 含 site 占位（含回填 TODO）+ image.remotePatterns(lain.bgm.tv) + mdx/sitemap/icon 集成。
- content.config.ts 两集合分层 schema（bgmId 预留 / status enum / cover union）落定。
- lib/tools.ts、lib/anime.ts 收口数据读取；首页经 lib 渲染，不直接 getCollection。
- dist/index.html 含站名「初曦的窝」+ 种子标题；BaseLayout head 含 is:inline 主题脚本。
- 部署后（Task 4/5）：生产域名可访问，site 回填真实域名，暗色刷新首屏无 FOUC + localStorage 记忆保留。
</verification>

<success_criteria>
INFRA-01（Astro+Tailwind 静态架构 + git 自动部署 Vercel）、INFRA-02（两集合类型安全分层数据模型 + bgmId 预留）、INFRA-04（亮/暗切换 + 记忆 + 无 FOUC）达成。成功标准 2（主题记忆无 FOUC）由 Task 3 + Task 5 验证；成功标准 4（改文件 + git 自动部署 + schema 校验）由 Task 1-2 构建管道 + Task 4-5 部署链路验证。
</success_criteria>

<output>
Create `.planning/phases/01-foundation-shell/01-01-SUMMARY.md` when done
</output>

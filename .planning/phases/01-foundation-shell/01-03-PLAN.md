---
phase: 01-foundation-shell
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/layouts/BaseLayout.astro
  - src/pages/tools/index.astro
  - src/pages/anime/index.astro
  - src/pages/404.astro
  - public/og-default.png
autonomous: true
requirements: [INFRA-06]

must_haves:
  truths:
    - "访客访问 /tools 与 /anime 不 404，见空状态文案与「回到首页」入口"
    - "访客访问不存在路径见 404「走丢」文案与回首页入口"
    - "每页 <head> 输出 <title> 模式「{页面标题} · 初曦的窝」、description、Open Graph、canonical"
    - "构建产出 sitemap-index.xml（依赖 01-01 已设的 site + sitemap 集成）"
    - "页面未传 ogImage 时回退到站点级默认 OG 图 /og-default.png"
    - "[决策覆盖] 落实 D-02（SEO title 模式「{页面标题} · 初曦的窝」）、D-08（/tools 与 /anime 空状态可导航占位页，不 404）"
  artifacts:
    - path: "src/layouts/BaseLayout.astro"
      provides: "SEO head：title/description/OG/canonical，props title/description/ogImage?"
      contains: "og:title"
    - path: "src/pages/tools/index.astro"
      provides: "工具库可导航占位页（空状态）"
    - path: "src/pages/anime/index.astro"
      provides: "追番记录可导航占位页（空状态）"
    - path: "src/pages/404.astro"
      provides: "404 错误状态页"
    - path: "public/og-default.png"
      provides: "站点级默认 OG 兜底图（1200×630）"
  key_links:
    - from: "src/pages/tools/index.astro"
      to: "src/layouts/BaseLayout.astro"
      via: "title/description props"
      pattern: "BaseLayout"
    - from: "src/layouts/BaseLayout.astro"
      to: "Astro.site"
      via: "canonical + OG url 构建期拼接"
      pattern: "Astro\\.(site|url)"
---

<objective>
交付导航占位页与全站 SEO：BaseLayout 统一输出 SEO/OG/canonical head，`/tools`、`/anime` 空状态占位页，`/404` 错误页，站点级默认 OG 兜底图。sitemap 由 01-01 已装的 @astrojs/sitemap 构建期产出。

Purpose: 满足 INFRA-06（sitemap + 每页 SEO meta），保证首页两入口卡可导航不 404，站点可被搜索引擎收录。
Output: BaseLayout SEO 块 + 3 个页面 + 默认 OG 图。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-shell/01-UI-SPEC.md
@.planning/phases/01-foundation-shell/01-CONTEXT.md
@.planning/phases/01-foundation-shell/01-RESEARCH.md

<interfaces>
BaseLayout（01-01 创建）已含 `<head>` 内联主题脚本、字体 preload、slot、footer。本计划在其 head 追加 SEO 块，props 扩展为 `{ title, description, ogImage? }`。占位页/404 通过这些 props 消费 BaseLayout。
astro.config 的 `site` 与 `@astrojs/sitemap` 集成由 01-01 落定；本计划不修改 astro.config。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BaseLayout SEO head + 默认 OG 兜底图</name>
  <files>src/layouts/BaseLayout.astro, public/og-default.png</files>
  <read_first>01-UI-SPEC.md「Meta / SEO Contract」；01-CONTEXT.md D-02（title 模式）、D-05（site 依赖）；01-RESEARCH.md「SEO 组件 + sitemap」</read_first>
  <action>扩展 BaseLayout props 为 title/description/ogImage?。在 head 内计算：fullTitle = `${title} · 初曦的窝`（D-02）；canonical = new URL(Astro.url.pathname, Astro.site)；og = new URL(ogImage ?? '/og-default.png', Astro.site)。输出 <title>{fullTitle}</title>、<meta name="description">、<link rel="canonical">、og:type=website、og:title=fullTitle、og:description、og:url=canonical、og:image=og、twitter:card=summary_large_image。保留既有内联主题脚本与字体 preload，SEO 块置于其后。生成 public/og-default.png（1200×630，青蓝主色 #17B6D6 底 + 站名「初曦的窝」文字，本阶段占位质量即可，可用脚本或工具生成纯色带文字 PNG）。不修改 astro.config。</action>
  <verify>
    <automated>npm run build && grep -q 'og:title' dist/index.html && grep -q 'rel="canonical"' dist/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `BaseLayout.astro contains og:title`（grep 'og:title' src/layouts/BaseLayout.astro）
    - `dist HTML <title> 以 · 初曦的窝 结尾`（grep '· 初曦的窝</title>' dist/index.html）
    - `canonical link present`（grep 'rel="canonical"' dist/index.html）
    - `public/og-default.png exists` 且约 1200×630
  </acceptance_criteria>
  <done>每个构建页 head 含 title「… · 初曦的窝」、description、完整 OG、canonical；未传 ogImage 的页 og:image 指向 /og-default.png。</done>
</task>

<task type="auto">
  <name>Task 2: /tools 与 /anime 空状态占位页</name>
  <files>src/pages/tools/index.astro, src/pages/anime/index.astro</files>
  <read_first>01-UI-SPEC.md「Copywriting Contract」Empty state 行；01-CONTEXT.md D-08（占位页不 404）</read_first>
  <action>两页均用 BaseLayout。tools 传 title「工具库」，anime 传 title「追番记录」，各传贴切 description。主体渲染空状态：heading「这里还空空如也～ (｡･ω･｡)」，body「内容正在慢慢整理中，很快就会上线。先去逛逛其他角落吧！」，CTA「回到首页」链接 `/`（内部链接，无需 rel）。heading 用 --font-display，居中，套用 UI-SPEC 圆角/间距 token。完整浏览/筛选/详情属 Phase 2/3，本阶段仅保证路由存在可导航。</action>
  <verify>
    <automated>npm run build && grep -q '这里还空空如也' dist/tools/index.html && grep -q '这里还空空如也' dist/anime/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `/tools page HTML contains 这里还空空如也`（dist/tools/index.html）
    - `/anime page HTML contains 这里还空空如也`（dist/anime/index.html）
    - 两页含「回到首页」CTA 且 href="/"
    - `<title>` 分别为「工具库 · 初曦的窝」「追番记录 · 初曦的窝」
  </acceptance_criteria>
  <done>/tools、/anime 构建产出静态页，显示空状态文案与回首页入口，不 404，可从首页入口卡到达。</done>
</task>

<task type="auto">
  <name>Task 3: 404 错误页 + sitemap 产出验证</name>
  <files>src/pages/404.astro</files>
  <read_first>01-UI-SPEC.md「Copywriting Contract」Error state 行 + 404.astro 组件契约；01-RESEARCH.md「SEO 组件 + sitemap」</read_first>
  <action>404.astro 用 BaseLayout（title「页面走丢了」）。主体渲染错误状态：heading「诶呀，这一页走丢了 (｡•́︿•̀｡)」，正文「你要找的页面好像不存在，也许它还没被创造出来～」，CTA「回到首页」→ `/`。加一个二次元「走丢」小装饰 SVG（aria-hidden、pointer-events-none，克制点缀），复用 UI-SPEC 装饰规则。构建后确认 @astrojs/sitemap（01-01 已装）产出 dist/sitemap-index.xml。</action>
  <verify>
    <automated>npm run build && grep -q '走丢' dist/404.html && test -f dist/sitemap-index.xml</automated>
  </verify>
  <acceptance_criteria>
    - `404.astro contains 走丢`（dist/404.html 含「这一页走丢了」）
    - 404 页含「回到首页」CTA href="/"
    - `dist/sitemap-index.xml exists after npm run build`
    - 装饰 SVG 带 aria-hidden
  </acceptance_criteria>
  <done>访问不存在路径显示走丢文案与回首页入口；构建产出 sitemap-index.xml。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 无 | 纯静态只读页，无用户输入、无表单、无后端、无密钥 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Information Disclosure | canonical/OG url | accept | 仅暴露公开站点 URL（Astro.site），无敏感信息 |
| T-01-03-02 | Tampering | 页面 CTA 链接 | accept | CTA 均为内部相对链接 `/`；本计划无外链，无需 rel（外链安全在含外链的页强制） |

无 high 级需阻断项；本计划无 npm 安装、无用户输入、无 secret。
</threat_model>

<verification>
- `npm run build` 成功，dist 产出 tools/index.html、anime/index.html、404.html、sitemap-index.xml。
- 任一页 head 含 title「… · 初曦的窝」、description、og:title/og:description/og:image/og:type/og:url、canonical。
- 占位页与 404 文案逐字符匹配 UI-SPEC。
- 首页入口卡可到达 /tools /anime（与 01-02 集成，本计划保证目标路由存在）。
</verification>

<success_criteria>
INFRA-06 达成：sitemap-index.xml 构建产出 + 每页 SEO meta（title/description/OG/canonical）+ 默认 OG 兜底图；/tools、/anime、/404 可导航且文案符合契约。
</success_criteria>

<output>
Create `.planning/phases/01-foundation-shell/01-03-SUMMARY.md` when done
</output>
